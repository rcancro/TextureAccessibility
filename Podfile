# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'stupidtest' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for stupidtest
  #pod 'Texture', :git => 'https://github.com/TextureGroup/Texture.git', :branch => 'releases/p6.28', :inhibit_warnings => true
  pod 'Texture'

end
